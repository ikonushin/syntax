# 📖 SYNTAX Authentication v2.0 - Пользовательское руководство

**Обновлено:** 9 ноября 2025  
**Версия:** 2.0  
**Статус:** ✅ Готово

---

## 🎯 Краткое описание

Система аутентификации SYNTAX теперь использует:
- ✅ **JWT токены** для управления сессией
- ✅ **Реальную валидацию** учетных данных через OpenBanking API
- ✅ **Обновленный дизайн** с фирменной стилистикой
- ✅ **Плавные анимации** для улучшения пользовательского опыта

---

## 🚀 Быстрый старт

### 1️⃣ Запуск системы

```bash
# Перейти в папку проекта
cd /Users/mac/Desktop/projects/Syntax/Syntax-main

# Запустить все сервисы
docker-compose up --build -d

# Проверить статус
docker-compose ps
```

### 2️⃣ Открыть приложение

Откройте в браузере: **http://localhost:5173**

### 3️⃣ Вход в систему

**Используйте учетные данные OpenBanking Russia:**

```
Team ID:        team286
API Ключ:       DQXtm3ql5qZP89C7EX21QpPeHc4YSvey
```

---

## 📝 Форма входа

### Элементы формы:

**Team ID (client_id)**
- Ваш идентификатор в системе OpenBanking
- Пример: `team286`
- Обязательное поле

**API Ключ (client_secret)**
- Секретный ключ API из OpenBanking
- Защищен как пароль (скрыт)
- Обязательное поле

**Кнопка "Войти"**
- Отправляет учетные данные на сервер
- Во время проверки показывает анимацию загрузки
- Заблокирована во время процесса

### Обработка ошибок:

**❌ "Неверные данные авторизации"**
- Проверьте правильность Team ID и API Ключа
- Убедитесь, что ключ не истек
- Проверьте соединение с интернетом

**❌ "Ошибка при авторизации"**
- Сервер недоступен
- Попробуйте через несколько секунд

---

## 🎨 Дизайн и стиль

### Цветовая схема:
- **Фон:** Белый (#FFFFFF)
- **Акцент:** Золото (#FFD700)
- **Текст:** Темно-синий (#1A2233)

### Шрифт:
- **Oswald** (Google Fonts)
- Используется для всех элементов интерфейса

### Логотип:
- SVG формат
- Встроенный в форму
- Отображается вверху страницы

---

## ⏱️ Жизненный цикл токена

### JWT токен:
- **Срок жизни:** 30 минут
- **Хранилище:** localStorage браузера
- **Автоматическое обновление:** При новом входе

### Банковский токен (внутри JWT):
- **Срок жизни:** 24 часа (86400 сек)
- **Передается на сервер:** Не отправляется фронтенду
- **Используется сервером:** Для запросов к OpenBanking API

---

## 🔐 Безопасность

### ✅ Что защищено:

1. **Пароли никогда не логируются**
   - Логи не содержат client_secret
   - Только client_id логируется

2. **Токены защищены**
   - JWT хранится в localStorage
   - Недоступен для внешних скриптов (хотя не httpOnly)
   - Можно улучшить в продакшене

3. **Соединение защищено**
   - Используйте HTTPS в продакшене
   - Все запросы зашифрованы

4. **No hardcoded secrets**
   - Все переменные из .env
   - Нет тестовых ключей в коде

---

## 🧪 Тестирование

### Тест 1: Успешный вход

```bash
curl -X POST http://localhost:8000/api/authenticate \
  -H "Content-Type: application/json" \
  -d '{
    "client_id": "team286",
    "client_secret": "DQXtm3ql5qZP89C7EX21QpPeHc4YSvey"
  }'

# Ожидаемый результат (HTTP 200):
{
  "access_token": "eyJhbGci...",
  "token_type": "bearer",
  "expires_in": 1800,
  "bank_token_expires_in": 86400
}
```

### Тест 2: Неверные учетные данные

```bash
curl -X POST http://localhost:8000/api/authenticate \
  -H "Content-Type: application/json" \
  -d '{
    "client_id": "invalid",
    "client_secret": "wrong"
  }'

# Ожидаемый результат (HTTP 401):
{
  "detail": "Неверные данные авторизации"
}
```

### Тест 3: Проверить здоровье

```bash
# Backend
curl http://localhost:8000/health
# Ответ: {"status":"ok"}

# Frontend
curl -I http://localhost:5173
# Ответ: HTTP/1.1 200 OK
```

---

## 🔍 Проверка логов

### Просмотр логов backend:

```bash
# Все логи
docker-compose logs backend

# Только последние 20 строк
docker-compose logs backend --tail=20

# Только логи аутентификации
docker-compose logs backend --tail=50 | grep -i "authenticate"
```

### Пример логов:

```
INFO - Authenticating team team286
INFO - HTTP Request: POST https://sbank.open.bankingapi.ru/auth/bank-token "HTTP/1.1 200 OK"
INFO - Successfully authenticated team team286, token expires in 86400s
INFO - Encoded JWT token for client team286
INFO - Successfully authenticated team team286
```

---

## 📱 Мобильная версия

Форма полностью адаптивна:
- ✅ Работает на телефонах
- ✅ Оптимизирована для сенсорных экранов
- ✅ Шрифт увеличивается на экранах < 480px
- ✅ Кнопки достаточно большие (44px+)

**Рекомендуемый размер экрана:** минимум 320px

---

## 🐛 Решение типичных проблем

### Проблема: "Не могу вход в систему"

**Решение:**
1. Проверьте, что backend запущен: `curl http://localhost:8000/health`
2. Убедитесь, что используете правильные учетные данные
3. Проверьте соединение с интернетом
4. Перезагрузите страницу (F5)

### Проблема: "Ошибка при авторизации"

**Решение:**
1. Проверьте логи: `docker-compose logs backend --tail=50`
2. Перезагрузите контейнеры: `docker-compose restart backend`
3. Проверьте, что OpenBanking API доступен

### Проблема: "Токен истек"

**Решение:**
1. Это нормально - токен действует 30 минут
2. Просто войдите заново
3. Новый JWT токен будет создан

### Проблема: "Страница не загружается"

**Решение:**
1. Проверьте, что frontend запущен: `curl -I http://localhost:5173`
2. Очистите кэш браузера (Ctrl+Shift+Delete)
3. Откройте консоль (F12) и проверьте ошибки

---

## 🎬 Анимации

### При загрузке страницы:
1. Появление формы (fadeIn - 0.6s)
2. Сдвиг заголовка (slideDown - 0.8s)
3. Появление элементов по очереди (slideUp - 0.4-0.8s)

### При нажатии кнопки:
1. Появление спиннера загрузки
2. Кнопка становится неактивной

### При ошибке:
1. Красное сообщение с иконкой
2. Анимация shake (встряска)

### При успехе:
1. Перенаправление на следующий экран
2. Плавный переход

**Отключить анимации:** Если у вас включена опция `prefers-reduced-motion` в ОС, все анимации будут отключены.

---

## 🚀 Развертывание в продакшен

### Шаг 1: Обновить .env

```env
# Используйте уникальный ключ (минимум 32 символа)
JWT_SECRET=your-super-secure-production-secret-key-here-minimum-32-characters

# Используйте production URL
BASE_URL=https://sbank.open.bankingapi.ru

# Используйте production credentials
CLIENT_ID=your_production_client_id
CLIENT_SECRET=your_production_client_secret
```

### Шаг 2: Включить HTTPS

```bash
# Обновите docker-compose.yml для HTTPS
# Используйте Let's Encrypt сертификат
```

### Шаг 3: Настроить логирование

```python
# Обновите logging в backend
# Отправляйте логи в Sentry/CloudWatch/etc
```

### Шаг 4: Включить rate limiting

```python
# Ограничить количество попыток входа
# Например: 5 попыток за 5 минут
```

---

## 📚 Дополнительные ресурсы

### Документация:
- `AUTHENTICATION_IMPLEMENTATION.md` - Техническое описание
- `AUTHENTICATION_TECHNICAL_SPEC.md` - Полная спецификация
- `AUTHENTICATION_UPDATE_v2.md` - Описание обновлений

### API Endpoint:
- **POST** `/api/authenticate` - Вход в систему
- **POST** `/api/consents` - Создание согласия
- **GET** `/api/banks` - Список банков

### Внешние API:
- OpenBanking Russia: https://sbank.open.bankingapi.ru/docs

---

## ✅ Чек-лист для администратора

- [ ] Обновить .env с production JWT_SECRET
- [ ] Включить HTTPS
- [ ] Настроить логирование
- [ ] Установить rate limiting
- [ ] Протестировать на разных браузерах
- [ ] Проверить мобильную версию
- [ ] Обновить документацию
- [ ] Обучить пользователей
- [ ] Настроить мониторинг
- [ ] Создать backup

---

**Версия:** 2.0  
**Последнее обновление:** 9 ноября 2025  
**Автор:** GitHub Copilot  
**Статус:** ✅ Готово к использованию
